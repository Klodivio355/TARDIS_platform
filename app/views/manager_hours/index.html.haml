%h1 Listing Available Hours
%table
  %thead
    %tr
      %th Data Manager
      %th January
      %th February
      -#%th March
      -#%th April
      -#%th May
      -#%th June
      -#%th July
      -#%th August
      -#%th September
      -#%th October
      -#%th November
      -#%th December

  %tbody
    - @manager_hours.each do |manager_hour|
      %tr
        %td= manager_hour.email
        %td= manager_hour.available_hours
        %td= manager_hour.available_hours
%br

%h1 Listing Allocated Hours
%table
  %thead
    %tr
      %th Data Manager
      %th January
      %th February

  %tbody
    - @manager_hours.each do |manager_hour|
      %tr
        %td= manager_hour.email
        %td= manager_hour.allocated_hours
        %td= manager_hour.allocated_hours
%br

%h1 Listing Spare Hours
%table
  %thead
    %tr
      %th Data Manager
      %th January
      %th February

  %tbody
    - @manager_hours.each do |manager_hour|
      %tr
        %td= manager_hour.email
        %td= manager_hour.spare_hours
        %td= manager_hour.spare_hours
%br

%h1 Listing Holiday Hours
%table
  %thead
    %tr
      %th Data Manager
      %th January
      %th February

  %tbody
    - @manager_hours.each do |manager_hour|
      %tr
        %td= manager_hour.email
        %td= manager_hour.holiday_hours
        %td= manager_hour.holiday_hours
%br

= link_to 'New Manager hour', new_manager_hour_path
