%h1 Listing Users

%table
  %thead
    %tr
      %th Email
      %th Username
      %th Admin

  %tbody
    - User.all.each do |user|
      %tr
        %td= user.email
        %td= user.username
        %td= user.admin
%br

= link_to 'New Study', new_user_path
